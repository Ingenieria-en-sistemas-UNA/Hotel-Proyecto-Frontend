import React from 'react'
import { Grid } from '@material-ui/core'

export default props => {

    return (
        <Grid item >
            <h2>Inicio de Sesión exitoso (Componente proteguido)</h2>
        </Grid>
    )

}
